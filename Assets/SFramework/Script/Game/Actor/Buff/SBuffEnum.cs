namespace SFramework.Actor.Buff
{
    public enum SBuffUpdateTime
    {
        Add,
        Replace,
        Keep,
    }

    public enum SBuffRemoveTime
    {
        Clear,
        Reduce,
    }
}
